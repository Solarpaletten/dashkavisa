# Жизненный цикл браузера и работа с Cloudflare

**Версия документа:** v0.1  
**Критичность:** 🔴 ВЫСОКАЯ

---

## ⚠️ ВАЖНО ПРОЧИТАТЬ ПЕРЕД ЛЮБЫМИ ИЗМЕНЕНИЯМИ

Этот документ описывает **критические аспекты** работы автоматизации браузера. Несоблюдение описанных правил **гарантированно приведёт к неработоспособности** проекта.

---

## Проблема: Cloudflare защита

Сайт VFS Global использует **Cloudflare** для защиты от ботов и автоматизации.

### Что делает Cloudflare

1. Анализирует характеристики браузера
2. Проверяет поведение пользователя
3. Показывает CAPTCHA при подозрении на бота
4. Блокирует запросы от автоматизированных браузеров

### Как Cloudflare определяет ботов

- Отсутствие JavaScript-движка
- Характерные признаки headless-режима
- Отсутствие истории браузера
- Подозрительные User-Agent
- Слишком быстрые действия
- Отсутствие mouse movements

---

## Текущее решение

Проект использует **реальный браузер Chrome** с настройками, маскирующими автоматизацию.

### Применённые техники

```python
# Маскировка автоматизации
options.add_argument("--disable-blink-features=AutomationControlled")
options.add_experimental_option("excludeSwitches", ["enable-automation"])
options.add_experimental_option("useAutomationExtension", False)

# Удаление флага webdriver
driver.execute_script("Object.defineProperty(navigator, 'webdriver', {get: () => undefined})")

# Реалистичный User-Agent
options.add_argument("--user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64)...")
```

---

## 🚫 ЧТО НЕЛЬЗЯ ДЕЛАТЬ

### 1. Использовать headless режим

```python
# ❌ ЗАПРЕЩЕНО
options.add_argument("--headless")
options.add_argument("--headless=new")
```

**Причина:** Cloudflare легко определяет headless-браузеры и блокирует их.

### 2. Использовать режим инкогнито для обхода

```python
# ⚠️ ИСПОЛЬЗОВАТЬ С ОСТОРОЖНОСТЬЮ
options.add_argument("--incognito")
```

**Причина:** Инкогнито-режим не сохраняет cookies и профиль, что вызывает повторные проверки Cloudflare.

### 3. Удалять временные профили Chrome

```bash
# ❌ ЗАПРЕЩЕНО делать автоматически
rm -rf /tmp/chrome_profile_*
```

**Причина:** Профиль содержит cookies и данные сессии, которые помогают пройти Cloudflare.

### 4. Очищать cookies браузера

```python
# ❌ ЗАПРЕЩЕНО
driver.delete_all_cookies()
```

**Причина:** Cloudflare использует cookies для отслеживания прохождения проверки.

### 5. Делать действия слишком быстро

```python
# ❌ ПЛОХО
email_input.send_keys(email)
password_input.send_keys(password)
login_button.click()

# ✅ ПРАВИЛЬНО
email_input.send_keys(email)
time.sleep(random.uniform(0.5, 1.5))
password_input.send_keys(password)
time.sleep(random.uniform(0.5, 1.5))
login_button.click()
```

**Причина:** Мгновенные действия — признак бота.

### 6. Игнорировать проверку Cloudflare

**Причина:** Если Cloudflare показал виджет верификации, автоматизация не сможет его пройти без специальных решений.

---

## ✅ ЧТО НУЖНО ДЕЛАТЬ

### 1. Использовать реальный браузер

Всегда запускать Chrome в обычном режиме, не headless.

### 2. Сохранять профиль браузера

После успешной ручной авторизации профиль содержит данные, помогающие обойти повторные проверки.

### 3. Добавлять случайные задержки

```python
import random
time.sleep(random.uniform(1, 3))
```

### 4. Делать скриншоты при ошибках

Скриншоты помогают понять, что видит браузер (Cloudflare challenge, ошибка, и т.д.).

### 5. Проверять наличие Cloudflare challenge

```python
if "Checking your browser" in driver.page_source:
    logger.warning("Cloudflare challenge detected!")
    # Ждём или прерываем
```

---

## Жизненный цикл сессии

### Нормальный цикл

```
1. Создание драйвера
2. Переход на страницу логина
3. [Возможная проверка Cloudflare]
4. Ввод учётных данных
5. Клик на кнопку входа
6. [Возможная проверка Cloudflare]
7. Переход на dashboard
8. Выполнение действий
9. Закрытие драйвера
```

### При обнаружении Cloudflare

```
1. Создание драйвера
2. Переход на страницу логина
3. ОБНАРУЖЕН Cloudflare challenge
4. Сделать скриншот
5. Залогировать проблему
6. Вернуть ошибку
7. Закрыть драйвер
8. Уведомить пользователя о необходимости ручного входа
```

---

## Признаки проблем с Cloudflare

### В логах

```
Таймаут при входе в VFS Global
Не удалось найти элемент mat-input-0
Страница не загрузилась за отведённое время
```

### На скриншотах

- Виджет "Checking your browser..."
- Виджет "Verify you are human"
- Пустая страница
- Страница с ошибкой 403

---

## Возможные решения (для будущих версий)

Следующие подходы **НЕ реализованы** в v0.1, но могут быть рассмотрены:

1. **undetected-chromedriver** — библиотека, автоматически патчащая ChromeDriver
2. **Playwright со stealth-плагином** — альтернатива Selenium
3. **Persistent профиль** — сохранение профиля между сессиями
4. **Ручная авторизация с сохранением cookies** — периодический ручной вход
5. **Сервисы решения CAPTCHA** — платные сервисы (2captcha, anti-captcha)

---

## Процедура восстановления

Если автоматизация перестала работать:

### Шаг 1. Проверить логи

```bash
tail -100 logs/browser.log
```

### Шаг 2. Проверить скриншоты

```bash
ls -la logs/screenshots/
```

Открыть последние скриншоты и посмотреть, что видит браузер.

### Шаг 3. Ручной вход

1. Подключиться к серверу через VNC или X11
2. Открыть Chrome вручную
3. Перейти на VFS Global
4. Пройти все проверки
5. Авторизоваться
6. Закрыть браузер

### Шаг 4. Перезапустить бота

```bash
pm2 restart visabot
```

---

## Мониторинг

Рекомендуется периодически проверять:

- [ ] Логи на наличие ошибок
- [ ] Скриншоты на наличие Cloudflare challenges
- [ ] Успешность автоматических проверок

---

*Этот документ критически важен для стабильной работы проекта. Любые изменения в browser.py должны учитывать описанные ограничения.*
